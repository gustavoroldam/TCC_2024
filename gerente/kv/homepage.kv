<HomePage>:
    FloatLayout:
        canvas:
            Rectangle:
                source: "../imagens/desktop/fundo.png"
                size: self.size
                pos: self.pos

        Label:
            pos_hint: {"right":0.85, "top":0.83}
            size_hint: 0.7, 0.2
            color: 0,0,0,1
            text:"Gerente Desconectado"
            id: id_nome_vendedor

        ImageButton:
            source: "../imagens/sairB.png"
            on_release: app.mudar_tela("login")
            pos_hint: {"right":0.35, "top":1}
            size_hint: 0.5, 0.10

        LabelButton:
            canvas.before:
                Rectangle:
                    source: "../imagens/botao.png" if self.state == "normal" else "../imagens/botaoPrecionado.png"
                    size: self.size
                    pos: self.pos
            on_release: app.Opcoes("gvendedores")
            text: "ANALISAR VENDEDORES"
            pos_hint: {"right":0.4, "top":0.65}
            size_hint: 0.3, 0.15

        LabelButton:
            canvas.before:
                Rectangle:
                    source: "../imagens/botao.png" if self.state == "normal" else "../imagens/botaoPrecionado.png"
                    size: self.size
                    pos: self.pos
            on_release: app.Opcoes("gvendas")
            text: "ANALISAR VENDAS"
            pos_hint: {"right":0.4, "top":0.45}
            size_hint: 0.3, 0.15

        LabelButton:
            canvas.before:
                Rectangle:
                    source: "../imagens/botao.png" if self.state == "normal" else "../imagens/botaoPrecionado.png"
                    size: self.size
                    pos: self.pos
            on_release: app.Opcoes("gestoque")
            text: "ANALISAR ESTOQUE"
            pos_hint: {"right":0.9, "top":0.65}
            size_hint: 0.3, 0.15

        LabelButton:
            canvas.before:
                Rectangle:
                    source: "../imagens/botao.png" if self.state == "normal" else "../imagens/botaoPrecionado.png"
                    size: self.size
                    pos: self.pos
            on_release: app.Opcoes("cadastrar")
            text: "GERAR CUPONS"
            pos_hint: {"right":0.9, "top":0.45}
            size_hint: 0.3, 0.15

        LabelButton:
            canvas.before:
                Rectangle:
                    source: "../imagens/botao.png" if self.state == "normal" else "../imagens/botaoPrecionado.png"
                    size: self.size
                    pos: self.pos
            on_release: app.Opcoes("gfuncionarios")
            text: "CADASTRO DOS FUNCIONÁRIOS"
            pos_hint: {"right":0.4, "top":0.25}
            size_hint: 0.3, 0.15

        LabelButton:
            canvas.before:
                Rectangle:
                    source: "../imagens/botao.png" if self.state == "normal" else "../imagens/botaoPrecionado.png"
                    size: self.size
                    pos: self.pos
            on_release: app.Opcoes("gprodutos")
            text: "CADASTRO DOS PRODUTOS"
            pos_hint: {"right":0.9, "top":0.25}
            size_hint: 0.3, 0.15